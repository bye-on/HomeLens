<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.homelens.mapper.EmailVerificationTokenMapper">

  <insert id="insertToken" parameterType="com.homelens.model.EmailVerificationTokenDto"
          useGeneratedKeys="true" keyProperty="id" keyColumn="id">
    INSERT INTO email_verification_token (
      user_id, token_hash
    ) VALUES (
      #{userId}, #{tokenHash}
    )
  </insert>

  <select id="selectValidTokenForUpdate" parameterType="string" resultType="com.homelens.model.EmailVerificationTokenDto">
    SELECT id,
           user_id     AS userId,
           token_hash  AS tokenHash,
           created_at  AS createdAt,
           expires_at  AS expiresAt,
           used_at     AS usedAt
    FROM email_verification_token
    WHERE token_hash = #{tokenHash}
      AND used_at is NULL
    FOR UPDATE
  </select>

  <update id="markUsed">
    UPDATE email_verification_token
    SET used_at = now()
    WHERE id = #{id}
      AND used_at IS NULL
      <!-- AND revoked_at IS NULL -->
  </update>

  <!-- (선택) 재발급 시 기존 토큰 무효화 -->
  <update id="revokeAllByUserId" parameterType="long">
    <!-- UPDATE email_verification_token
    SET revoked_at = now()
    WHERE user_id = #{userId}
      AND used_at IS NULL
      AND revoked_at IS NULL -->
  </update>

</mapper>
